# This configuration packages all add-ons in their separate directories
# No package-as is specified because we want to keep all add-ons separate
# The packager will automatically include all directories with .toc files

externals:

ignore:
  - README.md
  - CLAUDE.md
  - update_addon.sh
  - .DS_Store
  - .git
  - .github
  - .gitattributes
  - .gitignore
  - PR_DESCRIPTION.md
  - .release